# PROJECT_STATUS.md - حالة المشروع وسجل المتابعة

## 1. الحالة العامة للمشروع
* **الإصدار الحالي**: `1.0.0+3` (Flutter Web)
* **المرحلة الحالية**: **مرحلة التدقيق الشامل للأداء وحفظ الذاكرة (Phase 7) - مكتملة**
* **حالة التوافق مع قاعدة البيانات**: متوافق تماماً مع بنية البيانات المرجعية وتم التحقق من سلامة احتساب الديون والتقارير بنجاح 100%.

---

## 2. المهام المنجزة
* [x] **إصلاح تجمد البحث في شاشة العقود** وتوسيع نطاق الاستعلام ليشمل الهواتف والمعدات الفرعية والأرقام التسلسلية بشكل مدمج ومحسن.
* [x] **إصلاح خطأ صلاحية لوحة التحكم للموظفين** في ملف `reports.php` ليتيح للموظفين استعلام إحصائيات لوحة التحكم إذا امتلكوا صلاحية الـ dashboard دون الحاجة لصلاحية التقارير العامة.
* [x] **إصلاح خطأ استيراد ClientsRepository** في صفحة عرض العملاء وتطهير بنية المجلدات من الكود المكرر والميت.
* [x] **إصلاح خطأ توافق الأنواع (Type Mismatch)** في استعلامات السندات.
* [x] **توثيق عقد العمل النهائي وقواعد التطوير** في ملف [AGENTS.md](file:///e:/rental_system/.agents/AGENTS.md).
* [x] **إنشاء ملف المتابعة المستمرة** `PROJECT_STATUS.md`.
* [x] **إنهاء مرحلة التدقيق الفعلي والاستكشاف (Discovery Phase)** وتوليد تقرير التدقيق الشامل [discovery_report.md](file:///C:/Users/Eng_Saleh/.gemini/antigravity-ide/brain/072d6885-f4b8-4e78-82db-1fb86a847b59/discovery_report.md).
* [x] **إتمام المرحلة الأولى (Phase 1)**: إزالة عمليات فحص البنية و DDL المتكررة من مسارات العقود والسندات في `helpers.php` و `rents.php` و `payments.php`.
* [x] **إتمام المرحلة الثانية (Phase 2)**: إزالة DDL المتكرر وعمليات Alter Table من مسارات المستخدمين والورديات والرواتب في `users.php` و `shifts.php` و `payroll.php`.
* [x] **إتمام المرحلة الثالثة (Phase 3)**: إزالة معالجة الإهلاك الشهري N+1 من مسارات الويب وحل حلقات N+1 للحضور والرواتب في `payments.php` و `reports.php` و `payroll.php`.
* [x] **إتمام المرحلة الرابعة (Phase 4)**: إصلاح المنطق المحاسبي لحساب مديونية العقود المفتوحة ديناميكياً وحل مشكلة الـ Full Index Scan بقائمة العملاء في `helpers_financial.php` و `clients.php`.
* [x] **إتمام المرحلة الخامسة (Phase 5)**: تحسين أداء استعلامات لوحة التحكم (Sargable)، وبناء الـ Rate Limiter للمصادقة وتوليد مفتاح JWT ديناميكي في `helpers.php` و `auth.php` و `dashboard.php`.
* [x] **إتمام المرحلة السادسة (Phase 6)**: تطبيق الترقيم والتصفح (Pagination) والبحث المعتمد على الخادم لعقود الإيجار في Flutter Web والـ API.
* [x] **إتمام المرحلة السابعة (Phase 7)**: التدقيق النهائي الشامل وإصلاح تسريبات الذاكرة وعرقلة الـ I/O وتحسين سلاسة التمرير بقائمة العقود.
* [x] **إصلاح ظهور "جاري..." بدلاً من مبلغ العقد** - السبب: `DateTime.tryParse` لا يتعامل مع صيغة AM/PM القادمة من API. الحل: استخدام `DateTimeUtils.parse` في `models.dart` و `rents_page.dart` لدعم كلا الصيغتين (24h و 12h AM/PM). التاريخ: 2026-07-18.
* [x] **إصلاح حجب الموظف عن شاشة العقود (403 Forbidden)** - السبب: دالة `has_permission` في `helpers.php` تعيد `false` إذا كان `permissions_json` فارغاً أو NULL دون الرجوع للإعدادات الافتراضية للدور. الحل: تعديل `has_permission` لاستخدام `normalize_user_permissions` للحصول على الصلاحيات الافتراضية. التاريخ: 2026-07-18.

---

## 3. المهام المتبقية
* [ ] تسليم المشروع للإنتاج الفعلي بعد اكتمال التدقيق والتحقق بنجاح 100%.

---

## 4. المشاكل المفتوحة والمحسنة

| المشكلة | التفاصيل | الحالة |
| :--- | :--- | :--- |
| **المشكلة 1** | فحص البنية المتكرر DDL على كل طلب HTTP. | 🟢 **تم الحل وتحسين الأداء** |
| **المشكلة 2** | حلقة التكرار N+1 للإهلاك داخل السندات والتقارير. | 🟢 **تم الحل (مفصولة ومقيدة بالـ CLI)** |
| **المشكلة 3** | DDL DROP INDEX المتكرر بالخلفية على جدول الإهلاك. | 🟢 **تم الحل وتفادي قفل الجداول** |
| **المشكلة 4** | ALTER TABLE ديناميكي أثناء تشغيل الرواتب والورديات. | 🟢 **تم الحل نهائياً** |
| **المشكلة 5** | خطأ محاسبي جسيم في حساب مستحقات العقود المفتوحة. | 🟢 **تم الحل والحساب اللحظي دقيق 100%** |
| **المشكلة 6** | استعلامات غير Sargable في لوحة التحكم. | 🟢 **تم الحل وتحديث الاستعلامات** |
| **المشكلة 7** | Full Index Scan في استعلام الديون بقائمة العملاء. | 🟢 **تم الحل وحوسب برمجياً بنظام الصفحات** |
| **المشكلة 8** | غياب تصفح العقود في Flutter Web. | 🟢 **تم الحل وتصفح العقود عبر السيرفر** |
| **المشكلة 9** | jank وتقطيع في التمرير بسبب DateTime parser في قائمة العقود. | 🟢 **تم الحل وتثبيتها بالموديلات** |
| **المشكلة 10** | غياب Rate Limiter للـ Auth ومفاتيح تشفير JWT ثابتة. | 🟢 **تم الحل وبناء الحظر الآمن والمفاتيح الديناميكية** |
| **المشكلة 11** | تسريب ذاكرة في سجل التدقيق لغياب dispose. | 🟢 **تم الحل وتفريغ الـ controller** |
| **المشكلة 12** | استعلامات N+1 لحساب حضور الموظفين بالرواتب. | 🟢 **تم الحل وتحويلها لـ Batch Query** |
| **المشكلة 13** | Async Disk Read IO Blocking في Dio Interceptor بـ Flutter. | 🟢 **تم الحل بتفعيل الـ Memory Cache** |

---

## 5. القرارات المعتمدة
* اعتماد استخدام بنية سجلات Dart (Dart Records) كمخرجات لـ `PaymentsRepository.list` والوصول لحقولها صراحة عبر `.items` و `.summary` و `.total`.
* اعتماد ملف `AGENTS.md` كمرجع أساسي غير قابل للاستثناء لجميع عمليات التطوير والصيانة في المشروع.
* فصل كود الهجرة والـ Schema DDL عن الطلبات التشغيلية ونقله كلياً لسطر الأوامر.
* حظر أي أوامر `ALTER` أو `CREATE TABLE` داخل مسار المعالجة الفعلي للـ API.
* منع حساب الديون الإجمالية عبر derived table JOIN على كامل الجدول في الاستعلامات المصفحة، واعتماد المعالجة البرمجية الذكية لمدى الصفحة.

---

## 6. سجل التعديلات
* **2026-07-18**:
  * **إصلاح تجمد البحث في شاشة العقود**: حل مشكلة اختفاء حقل البحث ولوحة المفاتيح أثناء تحميل نتائج البحث بصفحة العقود (عبر منع التدمير الكلي لـ Scaffold body وعرض LinearProgressIndicator)، وتحديث استعلام البحث بالـ API ليدعم البحث بالهاتف والرقم التسلسلي والمعدات الفرعية لجدول `rent_items`.
  * **إصلاح خطأ صلاحية لوحة التحكم للموظفين**: تعديل نظام التحقق من الصلاحيات في أعلى ملف `reports.php` بحيث يسمح للمسار `reports/dashboard` بالعمل في حال امتلاك المستخدم لصلاحية `dashboard` أو `reports` بشكل ديناميكي وفي استعلام واحد محسّن لقاعدة البيانات.
  * **إصلاح أخطاء بناء واجهة العملاء**: إضافة الاستيراد المفقود لـ `ClientsRepository` في ملف `ClientsView.dart` وحذف المجلد الفرعي المكرر والميت `lib/features/clients/clients` الذي كان يسبب أخطاء تحليل برمجية.
* **2026-07-12**:
  * **تعريب نتائج المتابعة**: تحديث وتصحيح منطق دالة `outcomeLabel` في الكلاس `CollectionFollowup` لتقوم بتعريب قيم نتائج متابعة التحصيل (مثل `follow_up_later` و `promise_to_pay`) وعرضها باللغة العربية بدلاً من النصوص الإنجليزية الافتراضية.
* **2026-07-11**:
  * **تطوير آلية اختيار المعدات عند فتح العقد**: استبدال نظام الصفوف والقوائم المنسدلة بنظام اختيار متعدد سريع (Multi-Select Checkbox) مع بحث وفلترة وترتيب ذكي وأزرار تحديد/إلغاء الكل، مع الحفاظ الكامل على التوافق مع الـ API وقاعدة البيانات والحسابات المالية.
  * إصلاح أخطاء التجميع لـ `hasScreenPermission` في واجهات (التقارير، السندات، وتفاصيل العميل) عن طريق استيراد ملحق الصلاحيات المفقود `permission_guard.dart`.
  * إصلاح خطأ المعلمة 'clearButtonProps' في صفحة السندات عبر نقلها إلى `suffixProps: DropdownSuffixProps` لتتوافق مع إصدار حزمة `dropdown_search` المحدث (v6.0.2).
  * تحويل حقل اختيار العميل في نافذة السندات إلى حقل بحث متقدم (Searchable Dropdown) باستخدام `DropdownSearch` ليتطابق مع بقية حقول البحث في النظام.
* **2026-07-09**:
  * **تطبيق المرحلة السادسة**: بناء Pagination و Server-side search للعقود في Flutter Web والـ API.
  * **تطبيق المرحلة السابعة**: تفعيل In-memory cache للرابط والتوكن، وتفريغ الـ Controller في سجل التدقيق المالي، وتحسين سرعة معالجة التواريخ في قائمة العقود لمنع jank التمرير.
  * إصلاح خطأ بناء كلاس `_RentsViewState` in `rents_page.dart` بإزالة الكود البرمجي المعلق والتالف من دالة التصفية السابقة.
  * إصلاح `rent_details_page.dart` و `client_details_page.dart` لحل مشكلة تعيين Record إلى `List<Payment>`.
  * كتابة ملف القواعد الأساسية للمشروع.
  * الانتهاء من مرحلة التدقيق الفني الشامل للمشروع.
  * **تطبيق المرحلة الأولى**: تحسين زمن استجابة الـ API وإيقاف فحوصات قاعدة البيانات التشغيلية.
  * **تطبيق المرحلة الثانية**: تعطيل فحوصات البنية و DDL وتعديلات الجداول في ملفات `users.php` و `shifts.php` و `payroll.php`.
  * **تطبيق المرحلة الثالثة**: حل مشكلة N+1 في حساب الإهلاك الشهري وتقارير الحضور ورواتب الموظفين في `payments.php` و `reports.php` و `payroll.php`.
  * **تطبيق المرحلة الرابعة**: تصحيح حساب ديون العقود المفتوحة ديناميكياً وإلغاء full table scans بقائمة العملاء في `helpers_financial.php` و `clients.php`.
  * **تطبيق المرحلة الخامسة**: جعل استعلامات لوحة التحكم sargable، تحويل JOIN العقود لـ LEFT JOIN لمنع اختفاء عقود V2، بناء Rate Limiter لحظر محاولات تسجيل الدخول بالـ IP وتوليد JWT Secret ديناميكي في `helpers.php` و `auth.php` و `dashboard.php`.


